@page
@model ShopModel

@* <h1>Handla</h1> *@

<div class="shop-input">
    <form method="post">
        <input type="text"
               asp-for="NewItem"
               placeholder="Skriv en vara..."
               class="input-field" />

        <button type="submit" class="add-button">
            <span>Lägg till</span>
        </button>
    </form>
</div>

@if (ShopModel.ItemList.Any())
{
    <ul class="shopping-list">
        @for (int i = 0; i < ShopModel.ItemList.Count; i++)
        {
            <li>
                @ShopModel.ItemList[i]

                <form method="post" asp-page-handler="Remove" style="display:inline">
                    <input type="hidden" name="index" value="@i" />
                    <button type="submit" class="remove-button">X</button>
                </form>
            </li>
        }
    </ul>
}
else
{
    <p>Inga varor i listan än.</p>
}

<div class="bottom-bar">
    <a href="/" class="add-button back-button">
        <span>T i l l b a k a</span>
    </a>
</div>
